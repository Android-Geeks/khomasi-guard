<resources>
    <!-- Base application theme. -->
    <style name="Theme.MySplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/darkBackground</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/dark_splash_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.CleanArchitectureEntityApp</item>
    </style>
    <style name="Theme.CleanArchitectureEntityApp" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor" >@color/dark_gray</item>
        <!-- Customize your theme here. -->
    </style>
</resources>