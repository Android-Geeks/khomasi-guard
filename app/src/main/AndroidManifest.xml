<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:name="com.company.khomasiguard.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/dark_splash"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/dark_splash_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:screenOrientation="portrait"
        android:theme="@style/Theme.MySplashScreen">
        <activity
            android:windowSoftInputMode="adjustResize"
            android:name="com.company.khomasiguard.presentation.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MySplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>